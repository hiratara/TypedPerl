- if、unless
- for、while、next、last
- 関数定義
-- 文の(≠式)のためにUnitと同じ振る舞いのUnknownを利用する
--- PerlではUnkown型の値はないことに注意
-- Unknownは;の前にのみ許される
-- 知見が増えた時点で、Unknownを正しい型に変えると良い

- 演算の優先順位
- 配列 (依存型にしたい)
--関数の引数@_は型がバラバラなのでタプルのほうが
- サブルーチンを配列の依存型を引数に(実体に近い)
-- アリティで多相的に呼び出せるようにする
-- これは結局部分型。型推論と相性悪そう
- 多相型 (+はDoubleとInt対応したい)。idも定義できるようlet多相
- リファレンス
- ビルトイン関数
- pretty printの改善
- 高階型サポート (Maybeとか)
- Double
