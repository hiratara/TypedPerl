- 関数の引数@_をrecord型にする
- 関数定義の型スキームの実装(idも定義できるようlet多相)
- クラスのサポート。(use Mooさせてメソッドをrecord型で)
- if、unless、for、while、next、last
-- 文の(≠式)のためにUnitと同じ振る舞いのUnknownを利用する
--- PerlではUnkown型の値はないことに注意
--- TypeVarにするの辞める。Statement、でもいいかも
-- Unknownは;の前にのみ許される
-- 知見が増えた時点で、Unknownを正しい型に変えると良い
- 高階型サポート (Maybeとか)
- 多相型 (+はDoubleとInt対応したい)。
- リファレンス
- Double
- ビルトイン関数
- prettyPrintが破綻してて (&ch)->($_[0]) みたいな状態
